# AI 활용 기록
> 프롬프트 원문보다 **왜 그렇게 물었는지**와 **결과를 어떻게 처리했는지**에 중점을 두었습니다.
---

## 1. 사용한 도구

| 도구 | 어디에 왜 썼는지 |
|---|---|
| **Claude (대화형 LLM)** | 요구사항 분석, 설계 검토, 코드 생성, 디버깅, 적대적 테스트 케이스 생성 — 전 과정 |
| **IntelliJ IDEA** | 편집 및 JDK 관리 — JDK 21 다운로드에 사용(홈 디렉토리 설치라 관리자 권한 불필요) |
| **Maven Wrapper (`mvnw`)** | 빌드·테스트 — 빌드 서버에 Maven이 없어도 동작하도록 |
| **Flyway** | 스키마 마이그레이션 — `V1__init.sql`이 제출용 스키마 문서를 겸함 |
| **JUnit 5 + AssertJ** | 파일명 검증 단위 테스트 44건 — `@ParameterizedTest`로 케이스 테이블화 |
| **curl + jq** | 배포 환경 실측 및 서버 검증 재현 — `scripts/verify.sh`로 스크립트화 |
| **Docker (postgres:16)** | 로컬 DB — 배포 환경과 동일한 PostgreSQL 버전 사용 |
| **Railway** | 배포 — Dockerfile 기반으로 빌드 환경 고정 |

> **스킬 / 플러그인 / MCP / 에이전트: 사용하지 않았습니다.**
> 대화형 LLM 단독으로 진행했으며, 이 규모의 과제는 별도 확장 없이
> 프롬프트 설계와 결과 검증만으로 충분하다고 판단했습니다.
>

---

## 2. 시간순 기록

---

### [01] 요구사항 분석 — 코드 생성 이전

- **의도:** 코드를 짜기 전에 요구사항 자체를 검토. 모순이나 함정이 있는지 확인
- **프롬프트:**
  > (과제 원문 전체 첨부) 이 과제에 대해 어떻게 생각해? 상세하게 분석해줘.
  > 함정 있는지도 파악해줘.
- **왜 이렇게 물었나:** "어떻게 구현해줘"가 아니라 "어떻게 생각해"로 물었습니다.
  구현 방법은 검색으로도 얻을 수 있지만 **요구사항의 결함**은 비판적 검토를 명시적으로 시켜야 나옵니다.
  "함정"이라는 단어를 넣어 긍정적 요약이 아닌 문제 지적을 유도했습니다.
- **채택한 것:**
    1. 과제 배경의 위협 모델("exe가 서버에 올라가 실행될 경우 위험")이 부정확하다는 지적.
       리눅스 서버에 `.exe`를 올려도 실행되지 않으며, 실제 위험은 재서빙 / webroot 내 해석 / 경로 조작입니다.
       → CONSIDERATIONS 0절의 출발점이자 이후 모든 설계 판단의 전제
    2. 고정 확장자 목록이 Windows 실행 파일 관점에 치우쳐 있고,
       웹 관점에서 더 위험한 `svg`(스크립트 실행 가능한 XML), `html`, `php` 등이 빠져 있다는 지적
    3. "누구나 접속 가능"과 "감사 로그(누가 언제)"가 서로 모순이라는 지적 → CONSIDERATIONS 3-2절
    4. 배포 플랫폼 제약(바디 크기, 휘발성 FS, 슬립)을 마지막에 만나면 사고가 난다는 경고
       → 배포를 가장 먼저 한 이유
- **채택하지 않은 것:**
    - 고정 확장자 목록에 `svg`, `html` 등을 임의로 추가하는 것.
      고정 목록은 과제가 명시한 요구사항이므로 변경하지 않고,
      **커스텀으로 추가 가능하다는 점**과 목록의 한계를 문서화하는 쪽을 택했습니다
    - 제안받은 시간 배분(총 20시간 가정) 중 일부.
      실제로는 배포와 로컬 환경 디버깅에 예상보다 많은 시간이 들었습니다
- **회고:** AI를 코드 생성기가 아니라 **요구사항 검토자**로 먼저 사용했습니다.
  이 단계가 없었다면 화면을 다듬는 데 시간을 쓰고,
  위협 모델 재정의라는 가장 큰 차별점을 놓쳤을 것입니다.

---

### [02] 스택 선정과 Spring Boot 4 리스크 선제 인지

- **의도:** 최신 stable을 쓸지, 자료가 많은 구버전을 쓸지 판단
- **프롬프트:**
  > https://start.spring.io/ — Spring Boot 프로젝트 생성은 어떤 설정으로 맞추는 게 좋겠어?
  > 자바 버전, Spring Boot 버전 등. 그리고 만들고 Railway에 원격저장소 연결해서 git push 하라는 거야?
  > 너 설명 자세히 좀 해. 지금 생략을 많이 한 거 같아.
- **왜 이렇게 물었나:** 앞선 답변이 단계를 건너뛰어 실행이 막혔습니다.
  "자세히"라고만 하지 않고 **"생략을 많이 한 것 같다"** 고 문제를 특정했습니다.
  이후 답변의 상세도가 크게 개선됐습니다.
- **확인 결과:** Spring Boot 3.x 계열은 OSS 지원 종료, 4.1.x가 현행 stable (Java 17~26 지원)
- **선제적으로 인지한 리스크:**
  Spring Boot 4는 Spring Framework 7 / Jakarta EE 11 / Jackson 3 기반이며 3.x API가 대거 제거됐습니다.
  **인터넷 자료와 LLM 학습 데이터 대부분이 3.x 기준**이므로,
  AI가 준 코드가 조용히 동작하지 않을 수 있다고 판단했습니다.
- **수립한 대응 원칙:**
    1. 프롬프트에 버전 명시 — "Spring Boot 4.1 기준으로, 3.x에서 바뀐 부분이 있으면 표시해줘"
    2. AI 답변보다 공식 레퍼런스(4.0 마이그레이션 가이드) 우선
- **실제 발생:** 빌드 로그에
  `ProbeExceptionHandler.java uses or overrides a deprecated API` 경고 확인.
  ※ TEST_GUIDE 항목 7 실행 후 구체 내용 기재
- **회고:** 리스크를 **먼저 예측하고 대응 원칙을 세운 뒤** 실제로 발생한 사례입니다.
  사후 수습이 아니라 사전 대비였다는 점에서 이후의 실패 사례들과 성격이 다릅니다.

---

### [03] 배포 환경 제약 실측 — 측정 스크립트의 조용한 실패 ⭐

- **의도:** 문서에 적힌 한도가 아니라 실제 값 확인.
  추정으로 문서를 쓰면 면접 당일 데모에서 어긋날 수 있습니다
- **접근:** 기능 구현 전에 측정 전용 엔드포인트(`/api/probe/status`, `/api/probe/upload`)를 먼저 배포.
  재배포 후 `/tmp` 잔존 여부와 DB 지연을 관측할 수 있도록 설계했습니다
- **받은 명령:** `head -c 1M /dev/urandom > test_1mb.bin`
- **겉보기 결과:** 1MB~100MB 전 구간 HTTP 200.
  이대로면 **"엣지 바디 제한 없음"** 으로 결론 낼 뻔했습니다
- **이상 감지:**

  | 크기 | 응답 시간 |
    |---|---|
  | 1MB | 0.29초 |
  | 100MB | **0.29초** |

  크기가 100배인데 시간이 같습니다. 도쿄 리전까지 100MB를 0.3초에 보내려면
  약 2.7Gbps가 필요한데 불가능합니다.
- **확증:** 서버 응답의 `size` 필드가 `0`. 실제로는 빈 파일을 보내고 있었습니다
- **원인:** macOS는 BSD `head`라 `1M` 접미사를 지원하지 않습니다(GNU coreutils 문법).
  게다가 `>` 리다이렉션은 명령 실행 **전에** 파일을 생성하므로,
  명령이 에러로 죽어도 **0바이트 파일이 남습니다.**
  추가로 `curl -s`가 stderr까지 숨겨 실패가 완전히 은폐됐습니다
- **조치:**
    1. `dd if=/dev/urandom of=test_${s}mb.bin bs=1m count=$s` 로 교체
    2. `ls -lh`로 **파일 크기를 먼저 검증**하는 단계 추가
    3. 측정 스크립트가 `local_size`와 `server_size`를 나란히 출력하도록 수정
    4. `curl -s` → `-sS` (조용하되 에러는 표시)
- **재측정:** `local=1048576 server=1048576` 일치 확인 후 본 측정 진행
- **회고:** AI가 준 명령이 **내 환경에서 조용히 실패**했는데,
  결과값(HTTP 200)이 그럴듯해서 성공으로 보였습니다.
  과제가 가장 경계한 "검증 없이 붙여넣기"의 실물 사례이며,
  **AI 답변의 검증이란 결과값을 믿는 게 아니라 수행 여부를 확인하는 것**임을 배웠습니다.

---

### [04] 유니코드 정규화 — AI 예측 반증 ⭐

- **AI 주장:** "macOS 파일시스템은 한글을 NFD(자모 분리)로 저장하므로 업로드 시 NFD가 도착할 것"
- **검증 명령:**
  ```bash
  echo hi > 한글파일.txt
  curl -sS -F "file=@한글파일.txt" $BASE/api/probe/upload \
    | jq -r .originalFilename | xxd | head -3
  ```
- **실측 결과:**
  ```
  00000000: ed95 9c ea b880 ed8c 8c ec9d bc 2e 74 7874
            (한)   (글)    (파)   (일)   .  t  x  t
  ```
  `ed 95 9c` = U+D55C(한) → **NFC 완성형. 예측과 반대입니다**
- **원인 규명:** HFS+는 파일명을 NFD로 강제 정규화했으나,
  **APFS(macOS 10.13+ 기본)는 정규화를 강제하지 않고 입력된 그대로 보존**합니다.
  AI의 지식이 구버전 파일시스템 기준이었습니다
- **그러나 결론은 유지했습니다:** APFS가 "강제하지 않는다"는 것은
  **NFD가 입력되면 NFD가 그대로 보존된다**는 뜻이기도 합니다.
  오래된 HFS+ 볼륨, macOS에서 만든 zip의 해제, 다른 앱이 생성한 파일 등에서
  NFD가 유입될 수 있으므로 혼재 가능성은 여전합니다
- **추가 검증:** ※ TEST_GUIDE 항목 5 실행 후 결과 기재
- **최종 판단:** 저장·비교 전 NFC 정규화 강제.
  근거는 **"맥이라서"가 아니라 "서버가 정규화하지 않으므로 클라이언트 환경에 따라 혼재하므로"** 입니다.
  또한 NFD는 자모가 분리되어 문자 수가 늘어나므로
  **정규화를 길이 검사보다 먼저** 수행하도록 순서를 정했습니다
- **회고:** "AI 말이 틀렸다"로 끝내지 않고 **왜 틀렸는지 규명한 뒤 결론은 다른 근거로 유지**했습니다.
  틀린 전제 위에 세워진 옳은 결론을 그대로 두면 나중에 무너집니다.
  이 과정의 부산물이 "정규화 순서" 설계 결정입니다.

---

### [05] 배포 시 DB 연결 실패 — 증상과 원인의 분리

- **증상:** Railway healthcheck 실패
- **첫 판단:** 헬스체크 설정 문제로 보였으나 로그를 역추적하니 인과가 반대였습니다

  ```
  DB 연결 실패 → Flyway 초기화 실패 → 스프링 컨텍스트 로드 실패
  → 앱이 아예 안 뜸 → /actuator/health 응답 없음 → 헬스체크 실패
  ```

  헬스체크는 **증상**이지 원인이 아니었습니다
- **근본 원인:** 스택트레이스 최하단
  ```
  java.net.UnknownHostException: 	postgres.railway.internal
                                  ↑ 콜론 뒤가 공백이 아니라 탭
  ```
  환경변수 `PGHOST` 값 앞에 **탭 문자가 혼입**되어 있었습니다.
  붙여넣기 시 딸려 들어간 것으로, 대시보드 화면상으로는 보이지 않았습니다
- **조치:** 변수 삭제 후 자동완성(`${{Postgres.PGHOST}}`)으로 재입력.
  손 입력을 배제해 재발 가능성을 제거했습니다
- **회고:** **보이지 않는 문자가 문자열 비교를 깨뜨리는 문제**입니다.
  파일명 검증에서 `file.exe `(후행 공백)를 처리해야 하는 이유와 **정확히 같은 유형**입니다.
  이 경험 이후 후행 공백·점 제거를 단순 요구사항이 아니라 실감되는 위험으로 다루게 됐습니다.

---

### [06] 테스트 우선 작성 (구현 전 전량 실패)

- **의도:** 실측으로 확정된 서버 동작을 기대 동작 명세로 전환.
  구현 후 테스트를 붙이면 "구현을 정당화하는 테스트"가 되므로 순서를 지켰습니다
- **프롬프트:**
  > 실패하는 테스트 만들자
- **접근:** 스텁 클래스(`UnsupportedOperationException`을 던짐)를 먼저 만들어
  **컴파일은 되고 실행하면 실패**하는 상태를 구성했습니다.
  이래야 "컴파일 에러"가 아니라 "빨간불"로 관측됩니다
- **설계:** 관심사를 둘로 분리
    - `FileNameNormalizer` — 정책을 모르는 순수 문자열 처리. DB 없이 테스트 가능
    - `ExtensionPolicyValidator` — 조각 + 차단목록 → 판정

  한 덩어리로 만들면 실패 시 어디가 문제인지 알 수 없습니다
- **결과:** 44건 작성, 구현 전 전량 실패 확인
- **커밋:** `test: 파일명 검증 케이스 추가 (구현 전, 전부 실패 상태)`

---

### [07] 테스트 파일 위치 오류 — 두 버그의 상호 은폐 ⭐

- **증상:** `./mvnw test` 결과 `BUILD SUCCESS`, `Tests run: 1, Skipped: 1`
- **판단:** 44건을 작성했는데 1건만 실행됐고 그마저 스킵.
  **실제 실행된 테스트는 0개** → 성공이 아니라 실패입니다
- **단서:** 로그의 컴파일 파일 **개수**
  ```
  Compiling 1 source file   ... to target/test-classes   ← 3이어야 함
  Compiling 10 source files ... to target/classes         ← 8이어야 함
  ```
- **원인 1:** 테스트 파일이 `src/main/java`에 위치
- **원인 2:** `pom.xml`에 `junit-jupiter-params`가 중복 선언되었고 `<scope>test</scope>` 누락.
  스코프 기본값이 `compile`이라 **main에서도 JUnit이 보여 컴파일 에러조차 나지 않았습니다**
- **핵심 판단:** **두 버그가 서로를 가려주고 있었습니다.**
  하나만 있었다면 컴파일 에러로 즉시 발견됐을 것입니다
- **조치:** 파일 이동 + 중복 의존성 제거
  (`spring-boot-starter-test`가 이미 JUnit 전체와 AssertJ를 포함)
- **결과:** `Tests run: 45, Errors: 44, Skipped: 1` — 의도한 빨간불 확인

---

### [08] AI 생성 테스트의 단언 오류 — 코드가 아니라 테스트가 틀린 사례 

- **상황:** 구현 후 44건 중 2건 실패 (`script.js`, `installer.bat`)
- **첫 반응 억제:** "테스트를 통과시키려면 코드를 어떻게 고치나"가 아니라
  **"코드가 틀렸나, 테스트가 틀렸나"** 를 먼저 물었습니다
- **원인:** `@ValueSource`로 7개 입력을 주면서 기대값은 `"exe"`로 **고정**되어 있었습니다
  ```java
  @ValueSource(strings = {"virus.exe", "script.js", "installer.bat", ...})
  void 차단_대상(String fileName) {
      assertThat(d.matchedExtension()).isEqualTo("exe");   // ← 고정
  }
  ```
  `.js` 파일이 `exe` 때문에 차단됐다고 단언하는 잘못된 테스트였습니다
- **판단:** **코드가 정확했습니다.** 테스트를 `@CsvSource`(입력·기대값 쌍)로 수정했습니다.
  만약 테스트에 맞춰 코드를 고쳤다면 — 예컨대 항상 `"exe"`를 반환하게 했다면 —
  **사용자에게 잘못된 차단 사유를 표시하는 버그**를 심을 뻔했습니다
- **부가 발견:** `@CsvSource`는 필드 앞뒤 공백을 자동으로 trim합니다.
  후행 공백 케이스(`'virus.exe '`)는 작은따옴표로 감싸야 의도가 보존됩니다.
  → **검증 대상 자체가 사라지는** 또 다른 조용한 실패 유형
- **커밋:** `test: 차단 확장자 단언 오류 수정 (@ValueSource → @CsvSource)`

---

### [09] AI와 무관하게 스스로 발견한 문제 ⭐

정책 CRUD API를 설계하다 발견했습니다. 요구사항에도 AI 답변에도 없던 항목입니다.

- **문제:** 관리자가 커스텀 확장자 입력란에 `.exe`(점 포함)를 입력하면
  DB에는 `.exe`로 저장되지만, 파일명에서 추출되는 조각은 `exe`이므로 **영원히 일치하지 않습니다.**
  즉 **차단을 등록했는데 차단되지 않는 상태**가 됩니다
- **왜 발견했나:** 검증 로직에서 확장자를 "점을 제외한 조각"으로 다루기로 결정했는데,
  입력 계층에서는 그 규약이 강제되지 않는다는 점을 인지했습니다.
  **두 계층의 데이터 형식 규약이 어긋나는 지점**을 찾은 것입니다
- **판단:** 검증 계층이 아니라 **입력 계층**에서 처리해야 할 문제입니다.
  정규화(trim → 선행 점 제거 → NFC → 소문자)를 형식 검증보다 **먼저** 수행
- **순서가 중요한 이유:** 검사를 먼저 하면 `.exe`가 `INVALID_FORMAT`으로 거부되는데,
  사용자가 관용적으로 점을 붙이는 것은 자연스러운 입력이므로
  **거부보다 흡수가 옳다**고 판단했습니다
- **검증:**
  ```bash
  curl -sS -X POST $L/api/extensions/custom -d '{"name":".SH  "}' | jq
  # → {"name":"sh","blocked":true}
  ```

---

### [10] 로컬 JDK 버전 불일치 — 진단 3회 실패 

- **증상 변화:**
    1. `UnsupportedClassVersionError: class file version 65.0 ... up to 61.0`
       → Java 21로 컴파일된 클래스를 Java 17 JVM이 실행
    2. `clean` 후 `release version 21 not supported` → 컴파일러 자체가 17
- **오진 1:** "`JAVA_HOME`이 비어 있을 것"
  → 실제로는 `corretto-17`을 명확히 가리키고 있었습니다
- **오진 2:** `java -version`으로 검증
  → **이 명령은 PATH를 보므로 `JAVA_HOME`을 확인할 수 없습니다.**
  검증 명령이 검증 대상을 읽지 않아 오진이 지속됐습니다
- **실제 원인:** `/usr/libexec/java_home -V` 결과
  ```
  Matching Java Virtual Machines (2):
      17.0.18 (arm64) "Amazon Corretto 17"
      17.0.18 (arm64) "Microsoft OpenJDK 17"
  ```
  **JDK 21이 시스템에 설치된 적이 없었습니다.**
  패키지 매니저는 설치 기록만 있고 실제 파일이 없는 상태였습니다
- **판단:** "설정이 틀렸나"가 아니라 **"대상이 존재하는가"** 부터 확인했어야 합니다.
  설치된 JDK가 전부 `~/Library/...`(IDE가 받은 것)에 있다는 점에서
  시스템 레벨 설치가 없음을 더 일찍 알 수 있었습니다
- **조치:** IDE로 JDK 21 다운로드 (홈 디렉토리 설치, 관리자 권한 불필요)
- **대비:** **로컬이 실행 불가한 동안에도 배포본은 정상 동작했습니다.**
  Dockerfile에 `FROM maven:3.9-eclipse-temurin-21`을 명시해
  빌드 환경을 코드로 고정했기 때문입니다.
  자동 감지 대신 Dockerfile을 택한 판단이 실제 상황에서 값을 했습니다

---

### [11] AI를 적대적 테스터로 활용

- **의도:** 내가 설계한 케이스는 내 사고의 한계 안에 있습니다. 외부 시각이 필요했습니다
- **프롬프트 설계:**
  > 아래는 파일 업로드 시 확장자를 차단하는 검증 로직이다.
  > 서버는 Linux, 파일은 저장하지 않으며, 업로드된 파일은 재서빙되지 않는다.
  > (FileNameNormalizer.java, ExtensionPolicyValidator.java 전문)
  >
  > 차단 목록이 {bat, cmd, com, cpl, exe, scr, js}일 때,
  > 이 검증을 통과하면서도 위험할 수 있는 파일명을 30개 만들어라.
  > 각 항목마다 **왜 통과하는지(코드의 어느 줄을 우회하는지)** 와
  > **통과 후 실제로 어떤 피해가 가능한지**를 함께 적어라.
  > **실제 피해를 설명할 수 없으면 그 항목은 제외하라.**
- **프롬프트에 담은 세 가지 장치:**
    1. **로직 설명이 아닌 코드 전문** — 설명만 주면 존재하지 않는 취약점을 상상합니다
    2. **환경 제약 명시** — Linux / 미저장 / 재서빙 없음. 해당 없는 공격을 사전 배제
    3. **"피해를 설명 못 하면 제외"** — 이론적 나열을 억제.
       이 조건이 없으면 걸러내는 노동이 전부 제 몫이 됩니다
- **도구:** 비가시 문자를 유니코드 코드포인트로 출력하는 `escape()` 헬퍼를 작성했습니다.
  [03][07][08]에서 겪은 조용한 실패 경험에서 도출한 도구입니다
- **결과 분류:** ※ TEST_GUIDE 항목 3 실행 후 기재

  | 분류 | 판정 기준 | 조치 |
    |---|---|---|
  | 진짜 우회 | 통과 후 실제 피해 경로 존재 | 정식 테스트로 승격 + 구현 수정 |
  | 표시 위장 | 실행은 안 되나 사용자를 속임 | 판단 후 문서화 |
  | 해당 없음 | 이 환경에서 미성립 | **이유를 적고 폐기** |

- **회고:** AI는 "가능한 공격"은 잘 나열하지만
  **"이 시스템에서 실제로 유효한 공격"** 은 구분하지 못했습니다.
  환경 제약을 대입한 취사선택은 전적으로 제 몫이었습니다.
  **채택한 것보다 버린 것과 그 이유가 판단 근거로서 더 중요하다**고 생각합니다.

---

## 3. 판단 근거 회고 (종합)

### 3-1. 그대로 쓴 것

| 항목 | 이유 |
|---|---|
| 위협 모델 재정의 ([01]) | 근거가 명확하고 실제로 검증 가능. 이후 모든 판단의 전제가 됨 |
| 확장자 추출 규칙 (점 분리 후 첫 조각 제거) | `readme`와 `.env` 두 함정을 규칙 하나로 해결. 44건 테스트로 검증 |
| 검증 순서 설계 (경로 → NFC → 길이) | 각 순서에 명확한 근거 존재 (`..` 오분류, NFD 문자수 팽창) |
| 단일 테이블 + `type` 컬럼 | UNIQUE 제약으로 교차 중복이 구조적으로 불가능해지는 이점이 큼 |
| Dockerfile 기반 배포 | [10]에서 실제로 값을 함 |

### 3-2. 고쳐 쓴 것

| 항목 | 어떻게 고쳤나 | 이유 |
|---|---|---|
| 측정 스크립트 `head -c 1M` | `dd`로 교체 + 크기 대조 로직 추가 | macOS 미지원, 조용한 실패 ([03]) |
| `curl -s` | `-sS`로 변경 | 에러 메시지까지 숨겨 실패를 은폐 |
| 테스트 단언 `@ValueSource` | `@CsvSource`로 교체 | 입력별 기대값이 달라야 함 ([08]) |
| `max-request-size` | file-size와 동일 → 여유 부여 (10MB / 12MB) | multipart 오버헤드로 경계값 파일이 거부됨 |
| `PGHOST` 환경변수 | 손 입력 → 자동완성 참조 | 탭 문자 혼입 방지 ([05]) |
| 커스텀 확장자 입력 처리 | 검증만 → 정규화 후 검증 | `.exe` 입력 시 차단 무력화 ([09]) |

### 3-3. 버린 것

| 항목 | 이유 |
|---|---|
| "macOS는 NFD" 전제 | APFS는 정규화를 강제하지 않음. **결론(NFC 강제)은 유지하되 근거를 교체** ([04]) |
| 고정 확장자 목록 확장 | 과제 명시 요구사항이므로 임의 변경하지 않고 한계를 문서화 |
| `junit-jupiter-params` 수동 선언 | 스타터에 이미 포함. 중복이 스코프 문제를 유발 ([07]) |
| Testcontainers 도입 | 시간 대비 비용. 트레이드오프 인지 후 문서화 (CONSIDERATIONS 6절) |
| 적대적 테스트 후보 일부 | ※ TEST_GUIDE 항목 3 후 기재 (예: NTFS ADS는 Linux에서 미성립) |

### 3-4. AI가 놓쳤거나 틀렸는데 직접 잡아낸 것

1. **[03]** macOS에서 조용히 실패하는 측정 명령.
   HTTP 200이라는 그럴듯한 결과가 실패를 은폐했고, **응답 시간의 물리적 불가능성**으로 발견
2. **[04]** "macOS는 NFD"라는 구버전 파일시스템 기준 지식.
   실측으로 반증하고 APFS 변경이라는 원인까지 규명
3. **[07]** 테스트가 0건 실행됐는데 `BUILD SUCCESS`.
   **컴파일된 파일 개수**를 세어서 발견
4. **[08]** AI가 생성한 테스트의 단언 오류. 코드가 아니라 테스트가 틀린 경우
5. **[09]** `.exe` 입력 시 차단이 무력화되는 문제. 계층 간 데이터 규약 불일치
6. **[10]** 검증 명령(`java -version`)이 검증 대상(`JAVA_HOME`)을 읽지 않아 오진이 지속

---

## 4. 방법론 회고 — 조용한 실패(silent failure)

개발 과정에서 **"성공 신호가 나오지만 실제 작업은 수행되지 않는"** 실패를 4회 겪었습니다.

| 회차 | 겉보기 | 실제 | 발견 방법 |
|---|---|---|---|
| [03] | HTTP 200 | 0바이트 전송 | 응답 시간이 크기에 비례하지 않음 |
| [07] | BUILD SUCCESS | 테스트 0건 실행 | 컴파일된 파일 **개수** 확인 |
| [08] | 테스트 실패 | 코드는 정상, 테스트가 틀림 | 실패 메시지의 기대값 검토 |
| [10] | 셸 침묵 | 환경변수가 잘못된 값 | 대상 **존재 여부**부터 확인 |

**도출한 원칙: 신호(성공/실패)가 아니라 수행량을 확인한다.**

- `curl` — 로컬 파일 크기와 서버 수신 크기를 대조
- Maven — `Tests run: N` 숫자를 확인
- 비가시 문자 — 코드포인트로 출력
- 환경 설정 — "설정이 맞나"가 아니라 "대상이 존재하나"

**이 원칙을 설계에도 반영했습니다.**

파일 업로드 검증에는 동일한 위험이 존재합니다 —
**검증 함수가 항상 `true`를 반환해도 "업로드 성공"으로 보입니다.**
따라서 테스트가 "차단되었는가"뿐 아니라
**"어떤 확장자 때문에 차단되었는가"**(`matchedExtension`)까지 단언하도록 설계했습니다.
로직이 무차별로 차단하는 경우를 잡아내기 위함입니다.

같은 이유로 로그에도 `reason`과 `matched`를 구조화해 남깁니다.
운영 중에 "차단은 되는데 엉뚱한 이유로 차단되는" 상태를 관측할 수 있어야 하기 때문입니다.

---

## 5. AI 활용 총평

AI가 강했던 영역과 약했던 영역의 경계가 분명했습니다.

**강했던 것은 넓이입니다.** 요구사항의 모순([01]), 프레임워크 함정([02]),
공격 벡터 나열([11])처럼 "내가 모르는 것의 목록"을 만드는 데 유용했습니다.
혼자였다면 위협 모델을 의심할 생각조차 못 했을 것입니다.

**약했던 것은 내 환경과 현재 시점입니다.** macOS의 `head` 동작([03]),
APFS의 정규화 정책([04]), 실제로 설치된 JDK([10])처럼
**"지금 이 기계에서 무엇이 참인가"** 는 계속 틀렸습니다.
공통점은 전부 **일반론은 맞는데 내 환경에서는 아닌** 경우였다는 점입니다.

그래서 프롬프트보다 **검증 절차**를 개선하는 데 시간을 더 썼습니다.
같은 질문이라도 "이렇게 하면 되나?"가 아니라
"이렇게 했더니 이 결과가 나왔다, 이게 맞나?"로 물었을 때 진전이 빨랐습니다.
전자는 AI의 일반론을 받고, 후자는 내 환경의 실제 데이터를 넣기 때문입니다.

다시 한다면 **측정 도구를 먼저 검증하는 단계**를 처음부터 절차에 넣겠습니다.
[03]에서 이 교훈을 얻은 뒤로도 같은 유형의 실패가 세 번 더 있었는데,
매번 "결과가 그럴듯한지"가 아니라 "실제로 수행되었는지"를 물었다면 즉시 잡혔을 것들이었습니다.
