# 고려사항

> ※ `TEST_GUIDE` 참조 표시는 추가 측정 후 채워야 하는 자리입니다.
> ※ 이 문서의 실측 데이터는 배포된 환경에서 `curl`로 직접 측정한 값입니다.
> 측정에 사용한 스크립트는 `scripts/verify.sh`에 포함되어 있습니다.

---

## 0. 위협 모델 재정의

과제 배경에는 "`exe`, `sh` 등의 실행 파일이 서버에 올라가 실행될 경우 위험"이라고
기술되어 있습니다. 구현에 앞서 이 전제부터 검토했습니다.

**검토 결과, 이 위협 모델은 정확하지 않습니다.** 리눅스 서버에 `.exe`를 업로드해도
저절로 실행되지 않습니다. 파일이 업로드되었다는 사실만으로 실행 권한이 부여되거나
실행 컨텍스트가 생기지 않기 때문입니다.

실제 파일 업로드의 위험은 다음 경로에서 발생합니다.

| 위험 | 설명 | 본 구현의 대응 |
|---|---|---|
| **재서빙(re-serving)** | 업로드된 `.svg` / `.html`이 다른 사용자에게 다시 제공될 때 저장형 XSS. SVG는 이미지처럼 보이지만 스크립트가 실행되는 XML | 업로드 파일을 재서빙하지 않음 |
| **webroot 내 해석** | `.php`, `.jsp` 등이 해석기가 있는 경로에 저장될 때 | 파일을 저장하지 않음 |
| **경로 조작** | `../../` 로 의도하지 않은 위치에 기록 | `PATH_TRAVERSAL`로 거부 (실측 근거는 1절) |
| **압축 폭탄** | 작은 압축 파일이 전개 시 디스크 고갈 | 미구현 — 파일을 저장·전개하지 않으므로 현재 구성에서 미성립 (6절) |
| **자원 고갈** | 대용량 업로드로 디스크/메모리 점유 | 서버가 본문 수신 전 거절함을 실측 확인 (1절) |

또한 과제가 제시한 고정 확장자 목록(`bat`, `cmd`, `com`, `cpl`, `exe`, `scr`, `js`)은
**전부 Windows 실행 파일 관점**입니다. 웹 서버 관점에서 더 위험한
`svg`, `html`, `htm`, `php`, `jsp`, `jar`, `hta`, `vbs`, `ps1`, `msi` 등이 빠져 있습니다.
다만 고정 목록은 과제 요구사항이므로 임의로 변경하지 않고,
커스텀 확장자로 추가 가능하도록 두었습니다.

**결론: 확장자 차단은 방어의 본체가 아니라 여러 겹 중 하나입니다.**
실제 방어선은 (1) 파일 미저장 또는 랜덤 파일명 저장, (2) webroot 외부 저장,
(3) `Content-Disposition: attachment`, (4) `X-Content-Type-Options: nosniff`,
(5) 별도 도메인에서 서빙 입니다.
본 구현은 이 중 (1)을 택했고, 그 근거는 2-7절에 있습니다.

---

## 1. 배포 환경 제약 (실측)

추정이 아니라 배포된 환경에서 직접 측정한 값입니다.
측정 전용 엔드포인트(`/api/probe/*`)를 먼저 배포하고 `curl`로 측정했습니다.

| 항목 | 측정값 | 측정 방법 | 설계 반영 |
|---|---|---|---|
| 엣지 바디 한도 | 관측되지 않음 | 앱 한도(10MB)가 항상 먼저 거절하여 엣지 자체의 한도는 도달하지 않음 | 의도된 설계 — 엣지에서 잘리면 사용자가 에러 메시지를 받지 못하므로(1-1절), 앱 한도를 플랫폼 한도보다 낮게 유지하는 것이 목표였음 |
| 413 발생 지점 | **application** | 응답 본문에 `"source":"application"` 확인 | 내 에러 메시지가 도달함 |
| 413 전달 방식 | **HTTP/2 CANCEL** | `curl: (92) stream was not closed cleanly` | 브라우저는 본문을 못 읽을 수 있음 → 클라 선제 검증 필수 |
| 대용량 자원 점유 | **없음** | 100MB 요청이 0.3초에 종료 (전송 불가능한 속도) | 서버가 `Content-Length`만 보고 거절 → DoS 벡터 아님 |
| multipart 오버헤드 | file-size == request-size면 경계값 거부 | 5MB 파일이 5MB 한도에서 413 | request-size에 여유(12MB) 부여 |
| 컨테이너 FS | **휘발성** | `/tmp` 저장 후 재배포 → 소멸 확인 | 영구 저장 불가 → 미저장 결정 |
| Content-Type | **스푸핑 가능** | `.txt` 파일을 `type=image/png`로 전송 → 서버가 그대로 수신 | MIME은 검증 근거로 사용 불가 |
| 파일명 정제 | **전무** | `../../etc/passwd`가 한 글자도 안 바뀌고 도착 | 프레임워크가 막아주지 않음 → 직접 검증 |
| 유니코드 정규화 | **미수행** | NFC/NFD 모두 받은 그대로 전달 | 저장·비교 전 NFC 강제 |
| DB 지연 | 3ms | `/api/probe/status`의 `dbLatencyMs` | 내부 네트워크 확인 |
| DB 타임존 | UTC (`+00`) | `select now()` 결과 | 저장 UTC / 표시 로컬 |

### 1-1. 413이 브라우저에 도달하지 않는 문제

서버는 413과 함께 한글 메시지를 정상 반환합니다(`curl -i`로 확인).
그러나 **서버가 본문 수신 전에 스트림을 취소**하므로, 클라이언트가 아직 업로드 중인
상태에서 연결이 끊깁니다. 이 경우 브라우저의 `fetch()`는 413 응답을 파싱하지 못하고
네트워크 오류로 처리할 수 있습니다.

즉 **정성껏 작성한 에러 메시지가 사용자에게 도달하지 않고 "알 수 없는 오류"로 보입니다.**

→ 크기 검증을 **클라이언트(메시지 전달용) + 서버(강제용)** 이중으로 구성했습니다.
클라이언트 검증은 UX 장식이 아니라 이 실측 결과에 따른 필수 요소입니다.

---

## 2. 검증 / 보안 관점

### 2-1. 확장자만 믿어도 되는가

믿을 수 없습니다. 실측으로 Content-Type 스푸핑을 확인했고(1절),
확장자는 파일명의 일부일 뿐 내용과 무관합니다.

**대응: 파일 앞부분 바이트로 실행 파일 시그니처를 검사합니다.**
`FileSignatureInspector`가 PE(`MZ`), ELF(`\x7fELF`), Mach-O, Java class(`CAFEBABE`),
shebang(`#!`)을 탐지합니다.

**이 방식의 한계를 명시합니다.**

- `docx` / `xlsx` / `pptx` / `jar` / `apk`는 **모두 ZIP(`PK\x03\x04`)** 으로 시작합니다.
  시그니처만으로는 `jar`와 `docx`를 구분할 수 없습니다.
- `txt` / `csv` / `svg` 등은 시그니처 자체가 없습니다.
- 폴리글랏 파일(예: GIFAR — GIF 헤더 + JAR 본체)은 두 포맷 모두로 유효합니다.

따라서 **"허용 타입 판별"이 아니라 "명백한 실행 파일 탐지"로 범위를 의도적으로 좁혔습니다.**
완전한 타입 판별은 이 방식으로 달성 불가능하며, 달성한 척하는 것이 더 위험하다고 판단했습니다.

검증: `printf 'MZ\x90\x00...' > disguised.jpg` → `EXECUTABLE_CONTENT`로 거부됨.

### 2-2. 대소문자 / 이중 확장자 / 후행 문자

실측 결과 서버는 파일명을 전혀 정제하지 않습니다. 다음이 모두 원본 그대로 도착합니다.

| 입력 | 도착 | 처리 | 근거 |
|---|---|---|---|
| `FILE.EXE` | 대문자 보존 | 소문자 정규화 후 비교 | |
| `file.exe.` | 후행 점 보존 | 반복 제거 | Windows가 파일 생성 시 끝 점을 제거하므로 실제로는 `file.exe` |
| `file.exe ` | 후행 공백 보존 | 반복 제거 | 위와 동일 |
| `file.exe.txt` | 그대로 | **모든 점 조각 대조** | 아래 참고 |
| `archive.tar.gz` | 그대로 | `tar`, `gz` 두 조각으로 분리 | |

**이중 확장자에 대한 판단**

`file.exe.txt`는 Windows에서 실제로는 txt 파일이며 실행되지 않습니다.
그럼에도 **모든 조각을 검사하기로 결정**했습니다.

- 근거: 서버가 다른 조각을 해석하는 구성(예: 일부 웹서버의 다중 확장자 처리)에서 뚫릴 수 있고,
  차단 정책의 목적상 보수적인 쪽이 맞습니다.
- **인지한 트레이드오프: 오탐이 발생합니다.**
  `내보고서.exe백업.txt` 같은 정상 파일도 차단됩니다.
- 이 오탐을 모르고 방치한 것이 아니라, 알고 감수한 선택입니다.
  화이트리스트 방식으로 전환하면 이 문제가 사라지므로 5-3절에 확장 방향으로 기재했습니다.

### 2-3. 확장자 없는 파일 / 점으로 시작하는 파일

이 두 케이스는 흔한 구현 함정입니다.

| 방식 | `readme` | `.env` |
|---|---|---|
| `path.extname()` | `""` | **`""`** ← 검사 자체를 건너뜀 |
| `split('.').pop()` | **`readme`** ← 파일명을 확장자로 오인 | `env` |
| **본 구현** | `[]` | `[env]` |

**본 구현의 규칙: 점으로 쪼갠 뒤 첫 조각을 버린다.**

```
normal.txt     → [normal, txt]      → [txt]
readme         → [readme]           → []
.env           → ["", env]          → [env]
file.exe.txt   → [file, exe, txt]   → [exe, txt]
```

규칙 하나로 두 함정이 동시에 해결됩니다.
`.env`는 "무조건 차단"이 아니라 **"확장자 추출이 정상 동작하는가"** 가 핵심이며,
차단 여부는 관리자가 `env`를 목록에 등록했는지에 따릅니다.

### 2-4. 확장자 입력값 검증

**직접 발견한 문제:** 관리자가 커스텀 확장자 입력란에 `.exe`(점 포함)를 입력하면
DB에는 `.exe`로 저장되지만, 검증 시 추출되는 조각은 `exe`이므로 **영원히 일치하지 않습니다.**
즉 차단을 등록했는데 차단이 되지 않는 상태가 됩니다.

요구사항에 명시되지 않은 항목이며, 검증 로직 구현 후 정책 API를 설계하다 발견했습니다.

**대응 — 정규화를 검증보다 먼저 수행:**

```
"  .EXE  "  →  trim  →  선행 점 제거  →  NFC  →  소문자  →  "exe"
```

정규화 **후** `^[a-z0-9]{1,20}$` 검사. 순서가 중요합니다.
검사를 먼저 하면 `.exe`가 거부되는데, 사용자가 관용적으로 점을 붙이는 것은 자연스러운
입력이므로 거부보다 흡수가 옳다고 판단했습니다.

**3중 방어:**

| 계층 | 수단 |
|---|---|
| DB | `CHECK (name ~ '^[a-z0-9]{1,20}$')` |
| 애플리케이션 | 정규화 후 동일 정규식 검증 |
| 프론트 | `textContent` 렌더링 (아래) |

프론트에서 `innerHTML`을 쓰지 않는 이유: 입력 제한이 20자인데
`<svg onload=alert()>`가 **정확히 20자**입니다. 길이 제한만으로는 저장형 XSS를 막을 수 없습니다.

### 2-5. 서버 사이드 검증의 필요성

클라이언트 검증은 우회 가능함을 실증했습니다.

```bash
curl -F "file=@evil.exe;filename=safe.txt;type=image/png" $BASE/api/upload
```

`filename`과 `type`을 임의로 지정할 수 있으므로, 브라우저를 거치지 않는 요청에는
클라이언트 검증이 존재하지 않습니다.

→ **클라이언트 검증은 UX(즉시 피드백), 서버 검증은 보안(강제).** 역할이 다릅니다.
`scripts/verify.sh`로 서버 검증이 실제로 동작함을 재현할 수 있습니다.

### 2-6. 크기 제한

| 설정 | 값 | 근거 |
|---|---|---|
| `max-file-size` | 10MB | 일반 문서·이미지 첨부 시나리오 가정 |
| `max-request-size` | 12MB | multipart 오버헤드. 두 값을 같게 두면 경계값 파일이 거부됨(실측) |
| 클라이언트 선제 검증 | 10MB | 1-1절의 413 미도달 문제 대응 |

서버가 `Content-Length`만 보고 본문 수신 전에 거절하므로, 대용량 요청이
디스크나 메모리를 점유하지 않음을 실측으로 확인했습니다.

### 2-7. 원본 파일명 사용 위험 / 저장 전략

**결정: 검증만 수행하고 파일을 저장하지 않습니다.** 메타데이터만 로그로 남깁니다.

근거 세 가지:

1. **실측:** 컨테이너 파일시스템이 휘발성이므로 저장해도 재배포 시 소멸합니다.
   영구 저장을 하려면 오브젝트 스토리지가 필요한데, 과제의 핵심(정책 강제)과 무관한 비용입니다.
2. **보안:** 배포 URL이 누구나 접근 가능합니다. 업로드 파일을 저장하고 재서빙하면
   **공개 악성코드 호스팅**이 됩니다. 0절에서 정리한 최대 위험이 재서빙이므로,
   저장하지 않는 것이 가장 확실한 방어입니다.
3. **요구사항 충족 여부:** 과제는 "저장 위치/방식은 자유"라고 명시했으며,
   정상 파일에 대해 성공 응답(`ACCEPTED`)을 반환하므로 요구사항을 만족한다고 판단했습니다.

저장을 하게 될 경우의 설계도 함께 기재합니다(현재 미구현):
원본 파일명은 로그·감사용으로만 보관하고, 실제 저장 파일명은 UUID로 대체하며,
webroot 외부에 저장합니다. 측정용 `/api/probe/upload`가 이미 이 패턴으로 동작했습니다
(`/tmp/probe/{uuid}.bin`).

### 2-8. MIME 타입 스푸핑

실측 결과:

```json
// "hello"가 들어있는 test.txt를 type=image/png로 전송
{ "originalFilename": "test.txt", "declaredContentType": "image/png", "size": 6 }
```

`MultipartFile.getContentType()`은 **서버가 판별한 값이 아니라 클라이언트가 선언한 값**입니다.
검증 근거로 사용하지 않으며, 대신 2-1절의 시그니처 검사를 사용합니다.

### 2-9. 제어 문자 / 유니코드 위장 (요구사항 외 항목)

- **RTLO (U+202E):** `photo\u202Egnp.exe`는 화면에 `photo_exe.png`로 보입니다.
  사용자가 확장자를 오인하게 만드는 위장이므로 `CONTROL_CHARACTER`로 거부합니다.
- **NUL (U+0000):** 문자열 절단 공격에 사용되므로 거부합니다.
- 판정은 유니코드 카테고리 `\p{Cc}`(제어) + `\p{Cf}`(서식)로 수행합니다.
- **인지한 부작용:** `\p{Cf}`에는 ZWJ(U+200D)가 포함되어 결합 이모지가 든 파일명도 거부됩니다.
  ※ TEST_GUIDE 4-3 실행 후 판단(현행 유지 / ZWJ 예외 허용)과 근거를 기재.

### 2-10. 유니코드 정규화 (요구사항 외 항목)

한글 파일명은 NFC(완성형)와 NFD(자모 분리) 두 형태로 존재할 수 있습니다.
**화면에는 똑같이 "한글파일.txt"로 보이지만 바이트가 다르므로 서로 다른 문자열입니다.**

실측 결과 **서버는 어떤 정규화도 수행하지 않고 받은 그대로 전달**합니다.
따라서 정규화하지 않으면 중복 검사·검색·차단 목록 대조가 모두 깨집니다.

→ 저장·비교 전 **NFC 정규화를 강제**합니다.
길이 검사보다 정규화를 먼저 수행합니다. NFD는 자모가 분리되어 문자 수가 늘어나므로,
정규화 전에 길이를 재면 정상 파일이 길이 초과로 거부됩니다.

### 2-11. 파일명 길이

현재 `sanitized.length()` 기준 255자로 제한합니다.

**인지한 한계:** 이 값은 **UTF-16 문자 수**인데, 대부분의 파일시스템 제한은
**바이트 단위(통상 255바이트)** 입니다. 한글은 UTF-8에서 문자당 3바이트이므로
100자 한글 파일명은 300바이트가 되어 실제 저장 시 실패할 수 있습니다.
현재는 파일을 저장하지 않으므로 즉시 문제되지 않으나, 저장을 도입하면 바이트 기준으로
변경해야 합니다.

---

## 3. 정책 / 데이터 관점

### 3-1. 고정 확장자와 커스텀 확장자가 겹칠 때

**단일 테이블 + `type` 컬럼(`FIXED` / `CUSTOM`) 구조를 택했습니다.**

테이블을 둘로 나누면 "진실의 원천"이 둘이 되어 교차 중복을 애플리케이션 로직으로
막아야 합니다. 하나로 두고 `UNIQUE (name)` 제약을 걸면 **구조적으로 중복이 불가능**합니다.

커스텀 입력란에 `exe`를 입력한 경우의 UX:

```json
{ "error": "ALREADY_FIXED",
  "message": "고정 확장자 목록에 있습니다. 위 체크박스를 이용해 주세요." }
```

단순히 "중복입니다"가 아니라 **다음에 할 행동을 안내**합니다.

### 3-2. 정책 변경 이력 / 감사

**요구사항 간의 모순을 발견했습니다.**

과제는 "누가 언제 무엇을 바꿨는지"의 필요 여부를 묻는 동시에,
배포 사이트가 "누구나 접속 가능"할 것을 요구합니다.
**인증이 없는 상태에서는 의미 있는 actor(누가)를 식별할 수 없습니다.**

**현재 판단: 감사 로그 미구현.** 사유는 다음과 같습니다.

- 인증이 선행되지 않으면 "누가"를 기록할 수 없어 감사 로그의 핵심 가치가 사라집니다.
- IP/User-Agent만 기록하는 것은 공용망·프록시 환경에서 식별력이 낮습니다.
- 실운영 도입 시에는 **관리자 인증 → actor 식별 → 변경 전/후 값 기록** 순서가 되어야 하며,
  테이블은 `policy_change_log(id, actor, action, target_name, before, after, changed_at)`
  형태를 상정합니다.

단, **업로드 거부는 전량 로그로 남깁니다**(5-2절). 정책 변경 이력과 달리
업로드 거부는 actor 식별 없이도 공격 패턴 탐지에 가치가 있기 때문입니다.

### 3-3. 200개 / 20자 제한의 근거와 초과 시 UX

| 제한 | 적용 대상 | 처리 |
|---|---|---|
| 200개 | **커스텀만** (고정 7개는 별도) | 도달 시 입력창과 버튼을 **비활성화** |
| 20자 | 커스텀 확장자 이름 | DB `CHECK` + 서버 정규식 + `<input maxlength>` |

200개는 과제가 제시한 값이며, 커스텀 목록 옆에 `n/200`을 표시하는 화면 구성상
**커스텀만 카운트**하는 것으로 해석했습니다.

한도 도달 시 에러 메시지를 보여주는 대신 **입력 자체를 잠급니다.**
실패할 동작을 시도하게 두는 것보다 사전에 차단하는 편이 낫다고 판단했습니다.

`maxlength` 등 상수는 서버 응답(`maxNameLength`, `customLimit`)에서 받아 설정하므로
**상수가 한 곳(서버)에만 존재**합니다.

### 3-4. 대량 조회/저장 시 성능

**인덱스는 성능 목적이 아닙니다.**

테이블 최대 행 수는 고정 7 + 커스텀 200 = **207행**입니다.
이 규모에서 조회 성능을 위한 인덱스는 무의미합니다.
`UNIQUE (name)` 인덱스는 **성능이 아니라 정합성**을 위한 것입니다.

**실제 성능 이슈는 다른 곳에 있습니다.** 업로드가 발생할 때마다
`findAllBlockedNames()`로 DB를 왕복합니다. 업로드가 잦아지면 이것이 병목입니다.

**현재 판단: 캐싱 미구현.** 근거는 DB 지연이 실측 3ms이고 과제 규모(최대 207행)에서 병목이 아니기 때문입니다.
도입 시 설계는 **정책을 메모리에 캐시하고 변경 시 무효화**하는 방식입니다.
정책 변경이 조회보다 압도적으로 드물기 때문에 캐시 적중률이 매우 높습니다.

---

## 4. UX / 예외 관점

### 4-1. 차단 시 메시지 — 무엇이, 왜

| 상황 | 응답 | 설계 의도 |
|---|---|---|
| 차단 확장자 | `차단된 확장자입니다. (.exe)` + `matchedExtension: "exe"` | **어떤 확장자가 걸렸는지** 명시 |
| 경로 문자 | `파일명에 경로 문자가 포함되어 있습니다.` | |
| 제어 문자 | `파일명에 허용되지 않는 제어 문자가 포함되어 있습니다.` | |
| 시그니처 불일치 | `확장자와 무관하게 실행 파일로 판별되어 차단했습니다. (Windows 실행 파일(PE))` | 확장자를 바꿔도 소용없음을 전달 |
| 고정과 중복 | `고정 확장자 목록에 있습니다. 위 체크박스를 이용해 주세요.` | 다음 행동 안내 |

`matchedExtension`을 응답에 포함시킨 것은 사용자 안내 목적이자,
**검증 로직이 실제로 동작했는지 확인하기 위한 장치**입니다(7절 참고).

### 4-2. 로딩 / 에러 / 네트워크 실패

- 초기 로딩: "불러오는 중…" 표시 후 데이터로 교체
- 조회 실패: "목록을 불러오지 못했습니다. 새로고침해 주세요."
- 업로드 실패: 네트워크 오류와 서버 거부를 구분해 표시
- 서버 중지 상태에서 새로고침하여 실제 실패 상태를 확인했습니다.

### 4-3. 저장 실패 시 화면과 DB의 일관성

체크박스는 클릭 즉시 시각적으로 변합니다. PATCH가 실패하면
**화면은 체크됨 / DB는 미체크** 상태로 어긋납니다.

→ 실패 시 `cb.checked = prev`로 **이전 상태로 롤백**합니다.
커스텀 추가/삭제 후에는 낙관적 갱신 대신 `load()`로 서버에서 다시 읽어
**서버를 진실의 원천**으로 유지합니다.
응답 시간이 다소 길어지는 트레이드오프를 인지하고 정합성을 택했습니다.

---

## 5. 운영 관점

### 5-1. 새로고침 / 동시 편집 정합성

애플리케이션의 "조회 후 없으면 삽입" 방식은 두 탭에서 동시에 추가할 때 깨집니다.

→ 애플리케이션에서 먼저 걸러 **친절한 메시지**를 주되,
**최종 방어선은 DB `UNIQUE` 제약**입니다.
`saveAndFlush` + `DataIntegrityViolationException` catch로 처리하여,
경쟁 상태에서도 데이터 정합성이 깨지지 않습니다.

**인지한 한계:** 다른 탭에서 변경한 내용은 새로고침 또는 다음 조작 시점에 반영됩니다.
실시간 동기화(WebSocket/SSE)는 과제 규모 대비 과하다고 판단해 미구현.

### 5-2. 로그 설계

```
INFO  업로드 허용 | size=5 | name=sample.txt
WARN  업로드 거부 | reason=BLOCKED_EXTENSION | matched=exe | name=virus.exe
WARN  업로드 거부 | reason=PATH_TRAVERSAL | matched=null | name=../../etc/passwd
WARN  업로드 거부 | reason=EXECUTABLE_CONTENT | type=Windows 실행 파일(PE) | name=disguised.jpg
```

- **허용은 INFO, 거부는 WARN** — 모니터링에서 거부만 필터링 가능
- `reason`을 구조화하여 사유별 집계 가능
- `PATH_TRAVERSAL`은 정상 사용자가 만들 수 없는 파일명이므로 **공격 시도 신호**로 취급 가능

**로그 인젝션 방지(요구사항 외 항목):** 파일명에 개행 문자를 넣으면 로그에 가짜 줄을
삽입할 수 있습니다. 기록 전 제어 문자를 `?`로 치환하고 120자로 절단합니다.

### 5-3. 향후 확장

- **화이트리스트 전환:** 블랙리스트는 "모르는 위험을 막지 못하는" 근본 한계가 있고,
  현재 기본값이 전부 unCheck이므로 **초기 상태에서는 아무것도 차단하지 않습니다.**
  스키마에 정책 모드 컬럼을 추가하면 전환 가능하도록 여지를 남겼습니다.
- **사용자별 정책:** 현재 전역 단일 정책. 도입 시 `owner_id` 추가 및
  `UNIQUE (owner_id, name)`으로 제약 변경이 필요합니다.
- **감사 로그:** 3-2절 참고. 인증 도입이 선행 조건입니다.

---

## 6. 구현하지 않은 것과 그 이유

| 항목 | 판단 | 근거 |
|---|---|---|
| Testcontainers 통합 테스트 | 미도입 | 순수 로직은 DB 없이 단위 테스트 44건으로 커버. 정책 CRUD는 `verify.sh` 기반 재현 가능한 수동 검증으로 대체. 남은 시간 대비 비용을 고려한 선택이며, 동시성 시나리오가 자동 검증되지 않는 트레이드오프를 인지함(정합성 자체는 DB 제약이 보장) |
| 압축 폭탄 방어 | 미구현 | 파일을 저장·전개하지 않으므로 현재 구성에서는 성립하지 않음. 저장을 도입하면 필수 |
| 인증 / 권한 | 미구현 | 과제가 "누구나 접속 가능"을 요구. 3-2절의 모순 참고 |
| 파일 영구 저장 | 미구현 | 2-7절 참고 |
| Rate limit | 미구현 | 공개 URL이므로 실운영에서는 필요. 다만 과제 시연 목적의 배포이고, 업로드 파일을 저장하지 않아 자원 축적형 공격이 성립하지 않으며, 서버가 `Content-Length`만 보고 대용량을 거절함을 실측 확인. 인증 도입과 함께 다뤄야 할 항목으로 판단 |
| 접근성 / 반응형 | 부분 구현 | `<label>`로 체크박스 연결, 시맨틱 태그 사용, Enter 키 입력 지원, 삭제 버튼에 `title` 제공. 스크린리더 대응(`aria-live`로 에러 메시지 알림)과 키보드 포커스 관리는 미구현. 과제의 선택 항목이며 핵심 평가 영역이 아니라고 판단 |

**"고려하지 않음"이 아니라 "고려한 뒤 제외"입니다.** 각 항목의 도입 조건을 함께 기재했습니다.

---

## 7. 검증 방법론에 대한 회고

개발 과정에서 **"성공 신호가 나오지만 실제 작업은 수행되지 않는"** 실패를 반복적으로 겪었습니다.

| 회차 | 겉보기 | 실제 |
|---|---|---|
| 1 | HTTP 200 | 0바이트 파일 전송 (측정 스크립트가 macOS에서 조용히 실패) |
| 2 | BUILD SUCCESS | 테스트 0건 실행 (파일이 `src/main`에 위치 + pom scope 누락이 상호 은폐) |
| 3 | 테스트 실패 | 코드는 정상, 테스트의 단언이 틀림 |
| 4 | 셸 침묵 | `export JAVA_HOME=$(실패명령)` → 빈 값 설정 |

**도출한 원칙: 신호(성공/실패)가 아니라 수행량을 확인한다.**

- `curl`: 로컬 파일 크기 vs 서버 수신 크기 대조
- Maven: `Tests run: N` 숫자 확인
- 비가시 문자: 코드포인트로 출력하는 헬퍼 사용

**이 원칙을 설계에도 반영했습니다.**
파일 업로드 검증에는 동일한 위험이 존재합니다 —
검증 함수가 항상 `true`를 반환해도 "업로드 성공"으로 보입니다.
따라서 테스트는 "차단되었는가"뿐 아니라 **"어떤 확장자 때문에 차단되었는가"**(`matchedExtension`)까지
단언하도록 설계했습니다. 로직이 무차별로 차단하는 경우를 잡아내기 위함입니다.

---

## 부록. 테이블 스키마

`src/main/resources/db/migration/V1__init.sql` 참고.
Flyway로 관리되므로 스키마 정의가 곧 실행 가능한 코드입니다.

| 컬럼 | 타입 | 제약 | 비고 |
|---|---|---|---|
| `id` | `BIGSERIAL` | PK | |
| `name` | `VARCHAR(20)` | `UNIQUE`, `CHECK (~ '^[a-z0-9]{1,20}$')` | 소문자 정규화, 점 제외 |
| `type` | `VARCHAR(10)` | `CHECK IN ('FIXED','CUSTOM')` | |
| `is_blocked` | `BOOLEAN` | `NOT NULL DEFAULT FALSE` | 커스텀은 추가 시 `true` |
| `created_at` | `TIMESTAMPTZ` | `DEFAULT now()` | DB 시계 사용 |
| `updated_at` | `TIMESTAMPTZ` | `DEFAULT now()` | |

- `UNIQUE (name)`: 고정/커스텀 **교차 중복까지 구조적으로 차단**. 성능이 아니라 정합성 목적.
- `TIMESTAMPTZ`: 서버 타임존이 UTC임을 실측 확인. 저장은 UTC, 표시는 로컬.
- `created_at` / `updated_at`은 `insertable=false`로 두어 **애플리케이션 시계가 아닌 DB 시계**를 신뢰.
